# MistakeNote

## 기록 규칙
- 형식: `증상 / 원인 / 대응 / 재발 방지`
- 날짜는 `YYYY-MM-DD` 사용

## Entries

### 2026-02-24 - 아키텍처 전환 후 문서/코드 지연 동기화
- 증상: 문서는 Sheets 전환 기준인데 코드가 Supabase 상태로 남아 있었음
- 원인: 설계 결정 직후 실행 백로그/코드 전환이 즉시 연결되지 않음
- 대응: API/화면 우선 구현 후 문서 재동기화
- 재발 방지: 아키텍처 결정 시 `overview/context/todo/schema`를 같은 세션에서 갱신

### 2026-02-25 - dev lock 충돌
- 증상: `next dev` 실행 시 `.next/dev/lock` 충돌로 실행 실패
- 원인: 기존 dev 프로세스가 정리되지 않은 상태에서 재실행
- 대응: 잔여 node 프로세스 정리 후 재실행
- 재발 방지: 실행 전 기존 포트/프로세스 확인 습관화
