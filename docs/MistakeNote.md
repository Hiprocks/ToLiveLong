# MistakeNote

## 기록 규칙
- 기록 시각: `YYYY-MM-DD`
- 형식: `증상` / `원인` / `대응` / `재발 방지`
- 동일 실수 재발 시 기존 항목에 누적 업데이트

## Entries

### 2026-02-20 - DB 스키마 불일치
- 증상: 코드에서 `sugar`, `sodium` 값을 insert 하지만 DB 스키마에 컬럼이 없음
- 원인: 기능 확장 시 SQL 스키마 문서 동기화 누락
- 대응: Todo P0에 정합성 수정 작업 등록
- 재발 방지: 스키마 변경 체크리스트(코드/SQL/문서 동시 갱신) 도입

### 2026-02-20 - 텍스트 인코딩 깨짐 가능성
- 증상: 일부 한글 문자열이 깨져 보이는 현상 관찰
- 원인: 파일 인코딩 불일치(UTF-8 외 인코딩 혼재) 가능성
- 대응: Todo P0에 인코딩 통일 작업 등록
- 재발 방지: 저장 인코딩 규칙 UTF-8 고정 + PR 점검 항목 추가
